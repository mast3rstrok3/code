# Workflows, skills, and docs

The Settings catalog separates workflow automation into three layers:

- **Workflows** contains two Plan presets and three Engineering presets. **Wayfinder** is visible while it is under development but cannot start yet.
- **Skills** are focused engineering instructions. The catalog includes all 18 skills from Matt Pocock's engineering collection plus T3-native orchestration skills.
- **Docs** are supporting references used by those skills, such as ADR, context, testing, and browser-QA formats.

All three catalogs are read-only and versioned with the T3 Code server; the default model per step, on the same Workflows page, is yours to change. Skills are sorted alphabetically. Each skill shows a **Build** pill when it can be invoked directly and workflow pills for every guided workflow that uses it. Document pills link to the supporting references associated with that skill.

To invoke a skill directly, open the composer's model settings next to the model name, go to **Mode**, and choose **Skills**. T3 Code opens the skill catalog in a dialog. Select a skill to use it, or open its help button to read the full instructions first. The composer control then names the skill. That skill applies to the next turn and clears after the turn starts successfully. This path is built into T3 Code and works independently of provider-installed project or personal skills. `$skill` mentions still invoke skills discovered from the active provider.

## Choosing a workflow

Workflows live in the same composer control as skills. Open the model settings next to the model name, go to **Mode**, and choose **Workflows**. The dialog groups the presets under **Plan** and **Engineering**. Each help button shows the workflow steps. Selecting an available preset opens its model, cycle, review-part, and optional-stage settings before the composer switches modes. Changes made there apply only to that run. Build and Plan appear alongside these catalogs only while the legacy plan mode setting is on. Workflows and skills do not depend on it.

- **Quick Plan** uses the provider CLI's Plan mode, answers its questions automatically, builds and validates the change, then stops.
- **Fast Plan** adds the combined App Review, final Code Review, pull-request creation, and pull-request babysitting to Quick Plan.
- **Fast Engineering** runs the Engineering Workflow with ticket and combined App Reviews off by default.
- **Engineering Workflow** begins with Grill with Docs, then proceeds through Planning and the full Implementation workflow.
- **Wayfinder** will replace Grill with Docs with a Wayfinder planning phase. It is under development.

These names are presets, not fixed workflow types. You can turn any optional stage on or off before starting. Ticket App Review and the combined App Review have separate switches. Turning pull-request creation off also turns babysitting off; turning babysitting on turns pull-request creation on. The choices are stored with the run and do not change later runs.

Control the grill in the initial prompt using ordinary language: for example, “ask product questions only,” “focus on engineering decisions,” or “ask at most five questions.” Without a constraint, Grill with Docs covers the complete product, engineering, and domain frontier.

Before asking anything, Product Grill grounds itself in existing product context, resolves discoverable facts, and answers what is already clear. It maps the remaining product decisions as a dependency tree, then asks the complete currently unblocked product-alignment frontier when that frontier contains one through ten questions — the problem, the outcome, the audience, how it should feel, success criteria, scope, and non-goals. Ten is the maximum for a round, not a target; smaller frontiers keep their natural size, while larger frontiers continue in stable order after the first ten answers. Your answers unlock the next round. Product Grill never asks implementation, architecture, or testing questions.

Interactive Product and Engineering Grills present questions as structured cards above the composer on web, desktop, and mobile. Choices stay in their natural order, and each option keeps a neutral impact or tradeoff description. A separate callout names the recommended option and explains why it is preferred without changing the option label or description. You can use the composer field on the card for a custom answer. On web and desktop, the chevron in the card header folds a long question down to a single line so you can read the thread behind it, and the next question opens the card again. The final “lock it in or keep grilling” confirmation uses one question in the same structured card.

After a workflow starts, its Planning steps appear in the Workflows side panel even before child threads are created. Completed Planning artifacts remain available there. You can restart the Grill, Spec authoring, or Ticket authoring from that step's settings without resetting the shared worktree: existing commits and uncommitted files stay in place, while newly produced Planning artifacts supersede the earlier version as the workflow advances again.

## Phase details

**Quick Plan and Fast Plan** create a shared worktree and start the provider's native CLI Plan mode. T3 Code answers structured planning questions without pausing the run: it chooses a valid explicit recommendation, otherwise the first option, otherwise asks the agent to use its best judgment. The proposed plan launches Build in the same worktree. Quick Plan stops after focused validation unless you enable later stages. Fast Plan enables the combined App Review, final Code Review, pull-request creation, and pull-request babysitting by default.

**Fast Engineering** follows the same Planning and Implementation sequence as Engineering Workflow. It skips each ticket's App Review and the combined post-merge App Review by default. Code Reviews, pull-request creation, and pull-request babysitting remain enabled.

**Engineering Workflow** creates a prepared workspace and starts Grill with Docs against the submitted prompt. It continues through Spec authoring, durable tickets, ticket review, ticket implementation, ticket App Reviews and Code Reviews, merge, combined App Review, final Code Review, pull-request creation, and pull-request babysitting.

Its eleven phases are: prepare the shared worktree and App Dev Stack; Grill with Docs; author the Spec; author tickets; review and revise tickets; execute dependency-ready ticket waves; merge ticket branches; run Final App Review; run Final Code Review; create the pull request; and babysit the pull request.

The Fast Feature, Full Feature, standalone Planning, and standalone Implementation descriptions below are retained for existing runs. Standalone App Review remains available from the App Review panel and as a nested workflow stage, but it is no longer a composer preset.

**Fast Feature** has exactly four stages: Planning, Building, App Review, and Code Review. It creates its shared worktree and starts directly in Plan mode. The plan defines ordered Build workstreams and ordered App Review acceptance lanes. One Build thread runs every workstream. One durable browser reviewer runs every acceptance lane and owns UI interaction, recording, screenshots, findings, and the verdict. New temporary workspaces use a neutral `worktree-…` directory name; their Git branch is renamed to a short semantic name without a product prefix. T3 Code runs the repository-declared worktree setup and starts the App Dev Stack as soon as setup completes successfully; a setup failure prevents an unhealthy stack launch. Every agent turn launched in the worktree receives its exact path, current branch, live App Dev Stack state, and, once healthy, the authoritative frontend URL. Agents cannot substitute host containers, databases, or stacks from another worktree. Once the temporary worktree branch has its semantic name, the plan launches Build in that same worktree and reuses the already-owned stack. Browser App Review uses the frontend URL from that stack. Code Review runs its Standards and Spec passes in order, makes corrections, commits, and creates the pull request. PR creation is the handoff to a human. Failed or exhausted automated review is recorded on the pull request.

**Full Feature** — creates its shared worktree and starts Product Grill immediately. Its repository-declared setup runs in parallel, and its App Dev Stack starts as soon as setup succeeds. Product Grill is the only stage that asks you questions. Once you confirm what the product should do, the same thread enters an automatic Engineering Grill. The model grounds itself in the codebase, resolves the engineering and domain decision tree on its own without reopening product questions, and then continues automatically through Spec authoring, tickets, ticket review, and the full Implementation workflow in that same worktree, branch, and stack.

**Planning** — creates a prepared workspace and starts Grill with Docs against the submitted prompt. The grill honors user-supplied scope and question limits, uses the repository to resolve discoverable facts, and records assumptions when a limit leaves decisions unresolved. It then continues through Spec authoring, durable tickets, five ticket-review cycles by default, and Implementation in the same worktree, branch, and App Dev Stack.

**Implementation** — uses reviewed Planning tickets when they exist. Otherwise it turns the user prompt into one or more durable T3 tickets before writing code. Each ticket records whether App Review can verify it through the UI and, when it can, carries an attached review plan describing the surface, actions, assertions, and evidence. When the repository declares `e2eCommands`, ticket planning is e2e-first: a UI-verifiable ticket's acceptance criteria include extending the e2e suite with its flow, and the attached review plan covers only what those tests cannot prove. Each eligible ticket also declares how its review verifies it — `e2e` when the tests prove the whole flow, `browser` when they cannot cover it, or `both` — and the ticket's App Review runs exactly those parts: an e2e-only review skips the browser and needs no recording, while a repository without `e2eCommands` always reviews in the browser. **Settings → Workflows → Review parts** puts the same choice under your control as standing switches: one pair for App Review (standalone runs, the combined post-merge review, and the default for tickets) and one pair for Ticket App Review. A part turned off there is a prohibition, not a preference — it never runs even when a ticket asks for it, every review launch states its parts ("E2E tests: yes · Browser review: no"), and with both parts off the review step is skipped entirely, straight to Code Review with a recorded note. The same toggles appear in a running workflow's step settings menu in the Workflows panel; parts set there govern that run alone, outrank the standing Settings, and apply to the review cycles launched from then on. T3 stores these artifacts in the application rather than GitHub or repository scratch files.

The server starts every dependency-ready ticket at once, with no fixed worker cap. Each ticket has its own cadence, durable Implementation thread, branch, worktree, and ticket scope. It receives a separate App Dev Stack only when its effective review scope contains E2E tests or browser review. Recovery adds a continuation turn to the existing active thread. If that thread record is missing, the ticket stops for human attention without losing its branch or worktree. Inside one ticket, Implementation finishes before App Review starts, and App Review finishes before Code Review starts. Tickets do not block unrelated tickets from advancing. App Review may run ten cycles. Each cycle has one E2E and browser review thread, a gap-analysis thread when findings exist, and one TDD repair thread. A passing review stops early. Exhausting the review budget records unresolved findings and continues to Code Review. Ticket Code Review uses one fresh review-and-fix thread per cycle, stops early when clean, and runs at most five cycles. Fifth-cycle findings become a warning when the branch remains clean and usable. A successful ticket immediately records a durable stop request for its ticket stack; the shared workflow stack remains available through Final App Review. Broken prerequisites such as a stale or dirty worktree still stop the ticket.

After all tickets reach a terminal result, one merge-gate thread merges usable branches into the workflow's starting worktree and branch, resolves conflicts, and runs focused integration checks. Final App Review then uses the shared stack for up to ten cycles and concentrates on cross-ticket behavior. Final Code Review uses one fresh review-and-fix thread per cycle and stops clean or after five cycles. A clean cycle and cycle five run complete validation in that same thread. Review or validation exhaustion is attached to the workflow summary and pull-request body, and publication continues when Git still reports a clean, usable branch. A dirty or wrong branch, missing worktree, invalid commit, or publication failure stops automation.

**App Review** — the same review loop reaches you three ways. Fast Feature and Implementation launch it automatically; you can pick **App Review** in the composer's workflow list; and you can launch it from the web or desktop App Review panel. The review brief is required and remains the acceptance boundary for every cycle. When the repository's `t3.json` declares `e2eCommands`, every cycle starts by running those end-to-end test commands from the worktree, with `APP_REVIEW_PREVIEW_URL` set to the review's preview target so a suite that tests a deployed frontend hits the stack under review — including each ticket's own stack in per-ticket reviews. Each command gets its own check in the matrix (`e2e-1` for the first command, and so on), a failing command becomes findings for the gap analysis, and a pass requires every e2e check to have run fresh that cycle; e2e checks are never carried forward. The browser review then covers what the tests did not prove — acceptance criteria without coverage, visual and interaction quality, and the failures the run surfaced — instead of re-driving flows a passing test already exercises. The control beside the composer's mode picker holds everything else a launch needs, and the panel dialog asks for the same things. By default a review drives the App Dev Stack of the worktree it runs in. Name a **Review URL** to point it somewhere else, and it is used as given: `localhost:5173` and `staging.example.com` both work, and no App Dev Stack substitutes its own frontend for it. With the field empty, T3 Code resolves the matching App Dev Stack from the selected worktree before each review, falling back to a full `http://` or `https://` target in the review brief and then to the active Browser URL on that thread. Project Action preview URLs are not App Review targets because one project can contain worktrees with different app instances. Choose a budget of 1–10 complete cycles; **Settings → Workflows → App Review cycles** sets the standing default at 10. Older settings above 10 are read as 10. **Only review** replaces that budget with a single cycle that stops after its gap analysis: you get the browser review, its evidence, and the repair tickets for everything that did not work, and nothing is changed in the worktree. The run ends as **exhausted**, the same terminal any review reaches when it finishes with findings it could not verify as repaired, and its repair step cannot be started again from the panel. Each cycle has three ordered steps, each in its own thread: a fresh human-style Browser App Review using the UI, gap analysis and one persisted repair plan when the review fails, then implementation of that plan with the Implement skill. Only one of those step threads is active within a review cadence. The gap analysis works test-first: each repair ticket names the automated test that reproduces its gap, and the implementation writes that test, watches it fail, and repairs until it passes. Because each step is its own agent, each can be given its own model from the workflow’s **Models** settings. A pass completes the cycle after step one and requires a non-empty check matrix with every recorded check passed; deferred, blocked, failed, or not-applicable checks cannot close the run. Repair verification in the next cycle must also mark every earlier actionable finding passed by its original finding ID, and those are exercised in the browser again every time. What already passed is offered back to the next reviewer instead: it re-runs a passed check only when the repair could plausibly have broken it, and otherwise carries it forward, so the panel shows it as passed and carried from the cycle that ran it. Carried checks still count toward the matrix, so a pass cannot quietly drop one. If video finalization fails after actionable product defects were captured, screenshot-backed findings continue as **failed** with an evidence warning so the repair loop can self-heal; a clean pass still requires the complete recording. When the final review fails, its gap analysis and implementation still run so the budget always counts complete cycles; the run then ends as **exhausted** because no cycle remains to verify that final repair. A failed review, planning phase, or repair phase spends its current cycle after its one interrupted-launch retry. No eleventh cycle can start. Only a broken precondition ends the run early as **failed**: a worktree or preview target that cannot be resolved, a workspace that moved under the run, or an unattended review that hit an approval prompt.

Panel launches preserve the source conversation and temporarily lock its worktree and composer. The banner and panel both provide **Stop**. New-draft composer launches make the new top-level thread the controller; launches from an existing thread create a controller child without changing the source thread's normal workflow or interaction mode. Every cycle keeps its review document, recording, screenshots, gap analysis and plan, and implementation-thread link in the App Review panel. One-shot reviews created by older versions remain under Legacy history.

The Workflows panel's Interaction modes chips show the current durable path, including the focused ticket, App Review or Code Review cycle, and active phase. When several tickets run together, the chip shows the active count and focuses the first blocked ticket, then a ticket awaiting input, then running, paused, and upcoming tickets in wave order. Selecting the chip opens the workflow card, phase, step, wave, ticket, and cycle, then scrolls the current phase or thread into view. Ticket rows show one durable Implementation thread, three phase threads inside each App Review cycle, and one thread inside each Code Review cycle. Threads from older generations remain under **Earlier threads**.

A Browser App Review's live browser tab is temporary. It closes when the review or workflow child reaches a terminal state, and also when you stop the review's session, which ends its recording instead of leaving it running. Screenshots, recordings, findings, checks, and the verdict remain available as durable App Review evidence after the tab closes.

Reviews record the page itself rather than a video of it, and the App Review panel replays that recording with play and scrub controls. The recording is far smaller than a video and costs the machine much less while the review runs. Because it replays the page rather than pixels, an app drawn mostly in canvas or WebGL cannot be captured this way. T3 Code checks for that when a review starts and records video instead, and **Project settings → App Review recording** pins the choice for a project whose canvas only appears partway through a review.

**Wayfinder** is for efforts too large or uncertain to specify in one pass. Its durable Wayfinder Map appears above the Spec in the Planning side panel. Research, prototype, grilling, and task decisions are stored as normal Planning Tickets linked to the map, including their dependency edges. The map thread leaves research on those tickets for the ticket scheduler instead of starting background agents. It uses the Engineering Grill's interview and domain-modeling discipline while updating glossary and ADR documents as decisions crystallize. When the decision frontier is resolved, the map becomes the input to Spec authoring, and the flow continues through tickets, ticket review, and implementation.

## Navigating workflow threads

On web and desktop, a workflow keeps one sidebar entry: its top-level thread. Planning, build, review, repair, and other workflow-created child threads do not appear as separate sidebar rows.

Workflow-generated user messages show a **Skill · _name_** pill. Select it to open the associated instructions in the right panel, where you can read the formatted skill, switch to its exact raw text, and expand any supporting documents. Historical workflow messages receive the same association when their recorded turn metadata is available.

Open **Workflows** in the right panel to see the same ordered workflow steps defined in **Settings → Workflows**, together with every created child thread. Steps remain separate even when one conversation performs several of them. Repeating a review or repair appears as another numbered cycle inside the same step, not as a duplicate workflow step. Every step carries a settings button. If a run reaches a human gate, open the blocked step's settings and choose **Start step again**; T3 Code preserves the workflow run, worktree, branch, App Dev Stack, completed steps, and code, and resumes only that stage. **Stop step** in the same menu ends that step's active agent sessions and holds that part of the run there; the run, its worktree, commits, and history stay in place. A stopped wave, ticket, stage or step reads as **paused** in the panel, and its settings offer **Resume** in place of Stop. Nothing under a stopped scope starts again on its own, including work an agent had already queued before you clicked, and the rest of the run keeps going. Steps the workflow cannot re-enter on its own say so instead of offering an action that would do nothing. Selecting a child opens its conversation while keeping the complete workflow overview open. Select the workflow title to return to the top-level conversation. Provider-native subagents remain in the separate **Agents** surface.

Every row in the panel carries a colored dot and a word for what it is doing. Blue is running, amber is waiting on you to approve or answer something, red is a step that stopped and needs you, violet is paused, green is done, and a hollow dot has not started or was skipped. The live step also gets a colored rail down its left edge and a faint tint, so the one thing moving is easy to find without opening a phase. A collapsed phase shows how many of its steps are finished and names the step holding it up, and the workflow card above it does the same for the whole run, reporting the state that most needs you rather than an average.

Use **Pause** in the workflow panel to stop every active agent session in that workflow and prevent new turns from starting. The worktree, commits, uncommitted files, and workflow history remain in place, and the run stays paused until you say otherwise. Nothing restarts on its own, whichever scope you stopped: an agent that keeps writing for a few seconds after being told to stop does not count as the run waking up.

**Resume** in the same place picks the run back up at the step it stopped at, on the same worktrees, branches, App Dev Stack, and active stage thread. A completed review cycle still gets the next cycle's fresh thread. The step the run stopped at also offers **Start step again** in its own settings; that explicit action starts a new stage generation, while every other step says where the run is paused.

### When a provider fails or your plan hits its limit

Provider errors and plan usage limits can stop a turn mid-step. A workflow treats a retryable failure as blocked, not finished. The provider gets its own native retries first. T3 Code then retries once in the same thread and on the same model after about one minute. If that turn also fails and you selected a Recovery backup, T3 Code starts a clean provider session in the same thread with the backup model. Later retryable failures use that backup about every ten minutes for up to eight hours and at most 48 attempts. Provider-supplied retry times can extend those waits. A restart runs due recovery work but does not shorten a wait or restart the eight-hour clock.

The clean provider session keeps the T3 thread, messages, checkpoints, workflow ownership, worktree, branch, and stage claim. It receives the original phase assignment, a bounded tail of interrupted output, the workflow instructions, and directions to inspect the current Git state. Later workflow steps return to their usual model. If the backup is missing, disabled, removed, or the same as the primary model, recovery stays on the primary and records the reason.

Nothing retries while a turn is live, while the workflow is paused, or while the agent is waiting for an approval or user answer. Deliberate interruptions and authentication or configuration errors also do not retry. Older failures without structured retry details get one primary retry and one backup attempt. When recovery expires, the step returns to its normal failure handling. App Review recovery stays in the current phase thread and does not spend a review cycle or another durable thread ID.

### Choosing a model per step

By default every step runs on the model the workflow was started with. A step can have its own model only when it starts a separate thread. **Settings → Workflows → Default models for isolated steps** sets the standing choice for those steps. The workflow dialog exposes the defaults before a run starts. Inside a run, **Settings** at the top of the Workflows panel lays out the isolated steps at once, and each isolated step's settings button tunes that step. Those two edit the same per-run pins, and a per-run pin wins over the default.

**Recovery backup** chooses one fallback provider and model for failed recovery turns across the whole workflow. Set the standing choice in **Settings → Workflows**, or override it from the running workflow's Settings menu. **Auto** on a running workflow follows the standing choice. The backup is used only after the first T3 retry fails, and successful recovery does not change the model for later steps.

The **Quick model assignments** block sits above the detailed step list. **App Reviews and browser tests** applies one model to ticket reviews, final reviews, and their browser passes. **Code Reviews** does the same for ticket and final Code Reviews. If those steps already use different models, the quick assignment shows **Set all**. The detailed controls remain available for exceptions.

A step is a phase of the run, not a single agent. "Execute ticket waves" starts TDD workers, a ticket App Review, and a ticket Code Review. An App Review cycle runs a browser review, a gap analysis that writes the repair tickets, and the repair implementation. Each agent runs in a separate thread, so each can take its own model and follows its step unless you give it one:

- Leave an entry unset and it follows the level above. A sub-step follows its step, and a step follows the workflow's model.
- **Set** pins that entry to one CLI and model, using the same picker as the composer.

A pin set inside the panel belongs to that workflow run, applies to the next agent the step starts, and takes precedence over the Settings default. An unpinned isolated step, including the browser review, runs whatever model the workflow was started with. Settings defaults are keyed by the step's agent rather than by workflow, so setting the browser review once covers every workflow that runs one. Agents already running keep the model they launched with, so stop and start the step again to move work that is in flight. Pinning a step moves every agent it starts, including its per-ticket reviews. The final App Review and Code Review steps keep their own pins, so a ticket review and the final review of the same kind can differ. Shared-thread steps have no pin. In Planning, Grill with Docs, Spec authoring, and Ticket authoring use the model selected in the workflow thread's composer. Ticket review starts a reviewer thread and can have its own model. If the pinned provider is later disabled, the step falls back to the workflow's model and records why in the workflow thread.

### Choosing how often a step repeats

Three steps of a workflow loop rather than run once, and each has a cycle budget you can set:

- **Ticket review and revision cycles** — one reviewer pass over the Planning Tickets plus the revision it asks for. Five by default, up to 20. Planning completes with warnings when the budget runs out.
- **App Review cycles** — one browser review, the gap analysis that writes repair tickets, and the fix. Ten by default and ten at most. The review ends as exhausted when the budget runs out.
- **Ticket App Review cycles** — the same three phases, for the App Review a single ticket runs before its Code Review. Ten by default and ten at most.

They are set in the same two places as the models. **Settings → Workflows** holds the standing budget every new run starts from, and a running workflow's step settings button holds that run's own. A run's own budget wins.

Ticket and run-level App Reviews have separate budgets even though they use the same agent. Both accept 1 through 10 cycles. Raising a budget mid-run moves the App Review the run is already spending, so a review that ran out of cycles can be continued instead of restarted. Lowering a budget below what a review has already used does not undo those cycles; the review stops at its next decision point.

## Starting a step again

Work that failed does not have to sink the run. In the Workflows panel, a step's settings button offers **Start step again** for the merge gate, App Review, and Code Review, and each ticket in a wave carries its own **Re-run** for Implementation, App Review, and Code Review. Both start a fresh thread, so the attempt that failed stays readable next to the new one.

### Execution limits

A logical pass is one valid result from Implementation or Code Review. Each such stage may retry one interrupted launch or invalid response before it has a valid result. Findings and failed validation are results, so they do not start another review pass. Code Review findings may use the existing repair step and one final validation.

An App Review repair cycle is different. It contains review, gap analysis, and repair, and the workflow may run up to ten complete cycles. Each phase gets one retry if its launch is interrupted. A wrong branch or missing worktree stops automation immediately. Dirty files or a newer commit on the workflow's expected branch are treated as useful interrupted work: T3 Code sends the same worktree to a continuation agent to reconcile, validate, and commit before review starts again.

When automation stops, the panel shows **Needs human attention** with the ticket, stage, and reason. No later ticket, dependent wave, integration, review, or pull-request step starts automatically. Already-running sibling stages may finish and record results, but those results do not advance the run. **Start step again** is the only way to create a new generation. It resets only the selected stage and leaves all other stages closed.

Re-running a ticket's implementation also reopens the tickets that failed only because that one did. Those carried no work of their own, so they go back to waiting and run in dependency order once the re-run lands. Tickets that already succeeded are left alone, which is why a wave has no single re-run of its own.

An interrupted Implementation worker leaves its ticket branch and worktree in place. A continuation turn in the same thread sees its tracked changes, untracked files, commits, and conversation. Server restarts and lost provider sessions do not spend another thread ID. The same recovery applies to active App Review phases, Code Review cycles, the merge gate, and pull-request babysitting. Already reported results are replayed instead of discarded or rerun. Automatic cleanup preserves every thread with messages, activities, sessions, checkpoints, children, pending commands, or unfinished-workflow references. A workflow you deliberately paused remains paused.

An App Review cycle is three phases: the browser review, the gap analysis that writes the repair tickets, and the repair itself. Each phase row in the newest cycle carries its own **Re-run**. Redoing the gap analysis drops the repair it planned, since that repair no longer stands once its analysis is being redone. Redoing the browser review runs a new cycle instead: the cycle that disappointed you keeps its findings and its verdict, and the retry spends one of the budget. Earlier cycles are history and cannot be started again.

Starting a phase again accepts the worktree as it stands, including anything the repair you are redoing already committed. It is refused while the review itself is running, and while the ticket or step that owns the worktree has an agent of its own in it.

The **Re-run** popover carries the same model pin as the Models list, so choosing a model there both moves this attempt and sticks for later agents of that step. A stage that is still running cannot start again: stop it first. Two agents on one branch is the situation the guard exists to prevent.

A canceled implementation run normally stays terminal. Its final Code Review can start again when every ticket had already finished and the run preserved that review's exact integrated commit. This resumes review on the existing branch and worktree without reopening ticket work.

Every workflow run has a durable workflow ID. Children that belong to the run inherit that ID; a nested App Review receives its own ID and records the parent workflow ID. Use **Copy workflow link** in the Workflows panel to copy a URL that opens the top-level conversation, opens the workflow panel, and focuses that exact run.

## Stopping a workflow

Right-click the top-level workflow thread on desktop or long-press it on a touch device, then choose **Delete**. Confirming the deletion cancels active work and permanently removes the workflow thread together with all of its child threads and conversation history.

## Skills and docs

Settings → Skills lists the catalog alphabetically and shows its Build, workflow, and document relationships as pills. Settings → Docs groups each supporting reference under a related skill heading, summarizes its purpose, and shows every skill that uses it. Settings → Workflows names skill-backed steps after their canonical skills and keeps the step-specific action as context below the name.

Direct Build invocations bundle the upstream skill's supporting files with the selected skill for that turn. Guided workflow prompts continue to load their supporting docs on demand, keeping routine workflow turns focused while preserving access to detailed guidance.

The built-in grills share one Grilling primitive. Product Grill adds only codebase grounding and product-question scope. Engineering Grill adds the complete domain-modeling discipline used by Planning; Full Feature applies an automatic adapter to that same composition after Product Grill locks the intent. The question presentation follows Matt Pocock's useful separation of options, recommendation, and rationale, while T3 intentionally retains dependency-frontier batching instead of adopting an exactly-one-question-at-a-time rule. The direct Build catalog is a pinned snapshot of [Matt Pocock's engineering skills](https://github.com/mattpocock/skills/tree/main/skills/engineering). Guided workflows keep T3-specific adapters: specs and tickets are durable app artifacts instead of tracker issues, prototypes use real-application worktrees and app dev stacks, and App Review and the app dev stack are T3 additions.

Interactive grill threads use T3's own `workflow_request_user_input` tool, whichever agent runs them: Codex sees it as a dynamic tool, and Claude, Grok, and OpenCode reach it through T3's MCP server. Threads created before that tool existed temporarily fall back to the agent's own question tool and its three-question limit, which carries no recommendation. The visible Product or Planning workflow and its grill behavior do not become ordinary CLI Plan Mode. Full Feature's automatic Engineering Grill remains non-interactive and uses neither interactive transport.
